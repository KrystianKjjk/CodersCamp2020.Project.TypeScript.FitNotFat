import {generateWhiteButton, generateRedButton} from '../../../Src/UIComponents/Buttons/Buttons';

document.body.appendChild(generateWhiteButton('test', ()=>{}));
document.body.appendChild(generateRedButton('test', ()=>{}));